# 个人偏好配置文件
# 用于训练 AI 日报系统，使其100%匹配你的信息消费习惯
#
# 使用方法：
# 1. 填写下面的配置项
# 2. 运行 python scripts/generate_daily_report.py
# 3. 查看结果，通过 feedback.yaml 反馈
# 4. 系统会根据反馈持续优化

# ============================================================
# 第一部分：你是谁 (WHO)
# ============================================================
identity:
  # 主要角色（选择最匹配的）
  primary_role: "算法工程师"

  # 次要角色/兴趣
  secondary_roles:
    - "AI爱好者"
    - "效率提升爱好者"

  # 经验级别: junior / mid / senior / expert
  experience_level: "senior"

  # 技术栈（用于判断技术内容的相关性）
  tech_stack:
    - "Python"
    - "PyTorch"
    - "LLM"
    - "Transformers"

  # 当前工作领域
  work_domain: "AI应用开发"

  # 语言偏好: zh / en / both
  language_preference: "both"

# ============================================================
# 第二部分：你关心什么 (WHAT)
# ============================================================
interests:
  # 🔥 必看 - 错过会后悔的内容
  # 这些内容会被标记为「必读」，即使时间紧也要看
  must_track:
    - "LLM 能力突破（新模型发布、重大性能提升）"
    - "开源模型/工具发布（可以立即使用的）"
    - "AI Agent 框架和最佳实践"
    - "工程效率提升工具"
    - "Anthropic/OpenAI/Google 官方动态"

  # ⭐ 想看 - 有价值但可以稍后看
  want_track:
    - "AI 论文精读和解析"
    - "Prompt Engineering 技巧"
    - "行业趋势分析"
    - "知名研究者观点"
    - "性能优化和部署经验"

  # 📌 了解即可 - 扫一眼标题就够了
  nice_to_have:
    - "行业融资动态"
    - "AI 艺术/创意应用"
    - "一般性技术讨论"

# ============================================================
# 第三部分：你不想看什么 (NOT)
# ============================================================
filters:
  # 直接跳过的关键词
  skip_keywords:
    - "广告"
    - "抽奖"
    - "转发抽"
    - "薅羊毛"
    - "限时优惠"

  # 跳过的内容模式
  skip_patterns:
    - "纯娱乐/水帖（无实质内容的闲聊）"
    - "重复新闻（同一事件的多个转发）"
    - "过时信息（讨论已过时的模型/版本）"
    - "营销软文（过度宣传、夸大其词）"
    - "争议/八卦（人事纠纷、口水战）"

  # 可信来源（这些来源的内容优先级+1）
  trusted_sources:
    - "@emollick"
    - "@karpathy"
    - "@ylecun"
    - "@DrJimFan"
    - "@omarsar0"

  # 低优先级来源（除非内容特别相关，否则降级）
  low_priority_sources: []

  # 时效性要求（超过N天的内容降低优先级）
  freshness_days: 3

# ============================================================
# 第四部分：你怎么消费内容 (HOW)
# ============================================================
consumption:
  # 每日阅读预算
  time_budget: "15min"

  # 希望每天看到的内容数量
  daily_item_count:
    must_read: 5      # 必读不超过5条
    recommended: 15   # 推荐不超过15条
    fyi: 10           # 了解不超过10条

  # 内容详细程度
  # - headline_only: 只看标题
  # - summary: 标题 + 一句话摘要
  # - summary_with_context: 标题 + 摘要 + 关键引用
  # - full: 完整内容
  preferred_depth: "summary_with_context"

  # 是否需要「可立即行动」标记
  # 例如：新工具可以试用、新API可以接入
  action_oriented: true

  # 是否需要「为什么对你重要」的解释
  explain_relevance: true

  # 输出格式: markdown / html / json
  output_format: "markdown"

# ============================================================
# 第五部分：评分权重 (SCORING)
# ============================================================
scoring:
  # 各因素在最终评分中的权重 (总和=100)
  weights:
    topic_relevance: 35      # 主题相关性
    source_trust: 15         # 来源可信度
    actionability: 20        # 可行动性（能立即用）
    freshness: 15            # 时效性
    engagement: 10           # 社区热度（点赞/转发）
    uniqueness: 5            # 独特性（非重复）

  # 评分阈值
  thresholds:
    must_read: 80            # >= 80 分为必读
    recommended: 50          # >= 50 分为推荐
    fyi: 30                  # >= 30 分为了解
    skip: 30                 # < 30 分跳过

# ============================================================
# 第六部分：持续学习 (LEARNING)
# ============================================================
learning:
  # 是否启用反馈学习
  enabled: true

  # 反馈文件路径
  feedback_file: "configs/feedback.yaml"

  # 学习速率（每次反馈的影响程度）
  # - slow: 需要多次一致反馈才调整
  # - medium: 3-5次反馈后调整
  # - fast: 1-2次反馈立即调整
  learning_rate: "medium"
